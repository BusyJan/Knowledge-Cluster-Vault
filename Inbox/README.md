# Inbox

Fast capture before assigning a **project slug**.

## Rules

1. Nothing is canonical here — triage into `Projects/<slug>/` or `Archive/`.
2. Prefer capturing with a slug from the start (skip Inbox when possible).
3. Name files `YYYY-MM-DD - topic.md` for easy sorting.
